# Changes require a relaunch of Hyprland to take effect.

# Ensure installation bins are in the path
export MOONARCH_PATH=$HOME/.local/share/moonarch
export PATH=$MOONARCH_PATH/bin/:$PATH

# Set default terminal and editor
source ~/.config/uwsm/default

# Activate mise if present on the system
cmd-present mise && eval "$(mise activate bash)"
