#!/bin/bash

find ~/dotfiles/themes/ -mindepth 1 -maxdepth 1 \( -type d -o -type l \) ! -name "current" | sort | while read -r path; do
  echo "$(basename "$path" | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g')"
done
